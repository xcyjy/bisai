# 复制为 .env 并填入你的 Key。不填则自动使用「离线规则引擎」，demo 仍可跑通。
# Anthropic API Key（用 Claude 做高质量转换时需要）
ANTHROPIC_API_KEY=

# 使用的模型，默认 claude-opus-4-8
SCRIPT_MODEL=claude-opus-4-8

# 强制离线模式（true=不调用 LLM，纯规则转换，适合无网/无 key 演示）
FORCE_OFFLINE=false
